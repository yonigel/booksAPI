import { BookTitlePipePipe } from './book-title-pipe.pipe';

describe('BookTitlePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BookTitlePipePipe();
    expect(pipe).toBeTruthy();
  });
});
